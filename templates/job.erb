
Job {
	Name = "<%= name %>"
  	Client = <%= hostname %>-fd
	JobDefs = "<%= jobdefs %>"
	Write Bootstrap = "/var/spool/bacula/Client1.bsr"
}

Job {
	Name = "<%= name %>-res"
	Type = Restore
	Client = <%= hostname %>-fd
	FileSet = "FullSet"
	Storage = File
	Pool = Default
	Messages = Standard
	Where = /tmp/bacula-restores
}
