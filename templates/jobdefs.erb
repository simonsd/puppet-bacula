JobDefs {
  Name = "<%= name %>"
  Type = "Backup"
  Level = "<%= level %>"
  FileSet = "<%= fileset %>"
  Schedule = "<%= schedule %>"
  Messages = "<%= messages %>"
  Pool = "<%= pool %>"
  Priority = "<%= priority %>"
}
