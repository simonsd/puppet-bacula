JobDefs {
  Name = "<%= name %>"
  Type = "Backup"
  Level = "<%= level %>"
  FileSet = "<%= fileset %>"
  Schedule = "<%= schedule %>"
  Storage = "<%= storage %>"
  Messages = "<%= messages %>"
  Pool = "<%= pool %>"
  Priority = "<%= priority %>"
}
