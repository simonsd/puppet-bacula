JobDefs {
  Name = "<%= name %>"
  Type = "<%= type %>"
  Level = "<%= level %>"
  FileSet = "<%= fileset %>"
  Schedule = "<%= schedule %>"
  Storage = "<%= storage %>"
  Messages = "<%= messages %>"
  Pool = "<%= pool %>"
  Priority = "<%= priority %>"
}
