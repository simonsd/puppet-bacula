Pool {
  name                = "<%= title %>"
  label format        = "<%= title %>"
  pool type           = "<%= pool_type %>"
  recycle             = "<%= _recycle %>"
  autoprune           = "<%= _autoprune %>"
  purge oldest volume = "<%= _purge_oldest %>"
  maximum volume jobs = "<%= maximum_volume_jobs %>"
  maximum volumes     = "<%= maximum_volumes %>"
}
